import styled from 'styled-components';

export const Container = styled.div`
 
  div {
    display: flex;
    flex-direction: row;
    justify-content: space-around;
    
    section {
      margin: 1rem;
      width: 250px;
      padding: 0.5rem;
  }
}
`;
